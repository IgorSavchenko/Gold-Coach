window.addEventListener('load', () => {

  console.log("All resources finished loading!");
  var msnry = new Masonry( '.content__collage__gallery', {
    // options
    transitionDuration: '0.2s',
    horizontalOrder: true
  });

})
